# yohan-ai-dictionary — public site, own repo (Phase 3 template); not part of private Yohan OS snapshot
# leading slash = repo-root only, so SoT 경로 memory/projects/yohan-ai-dictionary/는 추적된다
/yohan-ai-dictionary/

# yohan-profile-card — separate Next.js side project, own repo; not part of private Yohan OS snapshot
/yohan-profile-card/

# Claude Code — local agent settings + worktrees
.claude/

node_modules/
dist/
.env
.env.*
!.env.example
*.local

# 텔레그램 봇 단일 인스턴스 락
memory/.telegram-bot.lock

# Tesseract 학습 데이터(로컬에 두었을 때만; tesseract.js는 기본적으로 캐시에 다운로드)
*.traineddata
git-autopull.log
*.log
.DS_Store
Thumbs.db
desktop.ini
~$*
